
export function TurboTax({setModal}) {
  return (
    <div className='modal-background'
        onClick={() => setModal(null)}>
      <div className='modal'
        onClick={(e) => e.stopPropagation()}>
       
      </div>
    </div>
  );
}

export function MilTax({setModal}) {
  return (
    <div className='modal-background'
        onClick={() => setModal(null)}>
      <div className='modal'
        onClick={(e) => e.stopPropagation()}>
       
      </div>
    </div>
  );
}

export function MilTaxConsultation({setModal}) {
  return (
    <div className='modal-background'
        onClick={() => setModal(null)}>
      <div className='modal'
        onClick={(e) => e.stopPropagation()}>
       
      </div>
    </div>
  );
}

export function NMCRS({setModal}) {
  return (
    <div className='modal-background'
        onClick={() => setModal(null)}>
      <div className='modal'
        onClick={(e) => e.stopPropagation()}>
       
      </div>
    </div>
  );
}

export function HouseFinder({setModal}) {
  return (
    <div className='modal-background'
        onClick={() => setModal(null)}>
      <div className='modal'
        onClick={(e) => e.stopPropagation()}>
       
      </div>
    </div>
  );
}